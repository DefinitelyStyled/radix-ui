import { createBoard } from '@wixc3/react-board';
import CollapsibleDemo from '../../../demo/collapsible-demo';

export default createBoard({
    name: 'Collapsible',
    Board: () => <CollapsibleDemo />
});
